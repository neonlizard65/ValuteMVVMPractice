<Page x:Class="ValuteMVVMPractice.Pages.GraphPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ValuteMVVMPractice.Pages"
      xmlns:oxy="http://oxyplot.org/wpf"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="GraphPage">
    <Grid>
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Vertical">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Выберите валюту:"/>
                    <ComboBox x:Name="ValuteBox" SelectedValue="{Binding ID}" SelectionChanged="ValuteBox_SelectionChanged"></ComboBox>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <DatePicker SelectedDate="{Binding DateRange1}" SelectedDateChanged="DatePicker_SelectedDateChanged"></DatePicker>
                    <DatePicker SelectedDate="{Binding DateRange2}" SelectedDateChanged="DatePicker_SelectedDateChanged"></DatePicker>
                </StackPanel>

                <oxy:PlotView x:Name="CurrencyPlot" Width="750" Height="300" HorizontalAlignment="Center"></oxy:PlotView>
            </StackPanel>

        </StackPanel>
      
    </Grid>
</Page>
